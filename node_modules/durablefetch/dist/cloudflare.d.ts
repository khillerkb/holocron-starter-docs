/// <reference types="@cloudflare/workers-types" />
import { DurableObject } from 'cloudflare:workers';
declare const _default: {
    fetch(req: Request, env: Env): Promise<Response>;
};
export default _default;
interface Env {
    DURABLE_FETCH: DurableObjectNamespace;
}
export declare class DurableFetch extends DurableObject {
    private state;
    private seq;
    private fetching;
    private live;
    private timeToLiveMs;
    constructor(state: DurableObjectState);
    fetch(req: Request): Promise<Response>;
    /** Check if there's an in-progress stream */
    private checkInProgress;
    /** Background: fetch upstream once, store + fan-out */
    private pipeUpstream;
    /** Delete all storage for this key */
    private deleteStorage;
    /** TTL alarm handler - clean up all stored data */
    alarm(): Promise<void>;
}
export declare function sleep(ms: number): Promise<void>;
export declare function headersToObject(headers?: HeadersInit): Record<string, string>;
//# sourceMappingURL=cloudflare.d.ts.map